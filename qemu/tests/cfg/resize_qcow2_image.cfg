- resize_qcow2_image:
    only Linux
    no RHEL.6 RHEL.7 RHEL.8
    no Host_RHEL.m6 Host_RHEL.m7 Host_RHEL.m8
    type = resize_qcow2_image
    virt_test_type = qemu
    login_timeout = 240
    must_packages = "guestfs-tools virt-install"
    new_image_size_increase = "+50G"
